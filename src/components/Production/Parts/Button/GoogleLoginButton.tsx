import { googleIcon } from "@/icons/icon";

export default function GoogleLoginButton() {
    return (
        <div className="flex flex-col justify-center items-center gap-1 cursor-pointer">{googleIcon} Google</div>
    )
}
